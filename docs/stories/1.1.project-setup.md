# Story 1.1: Project Setup & Development Environment

<!-- Powered by BMAD™ Core -->

## Status

**Done**

---

## Story

**As a** developer
**I want** a properly configured Next.js 15 project with TypeScript and essential tooling
**so that** the team can develop efficiently with type safety and code quality standards

---

## Acceptance Criteria

1. Next.js 15 App Router project initialized with TypeScript
2. Tailwind CSS 4.x configured with custom theme (KB-COM brand colors)
3. shadcn/ui installed with initial components (Button, Form, Card)
4. ESLint configured with TypeScript strict rules
5. Prettier configured for code formatting
6. Husky pre-commit hooks set up (lint, type-check)
7. Git repository created with initial commit
8. README.md with setup instructions
9. Environment variables template (.env.example)
10. VSCode recommended extensions and settings files

---

## Tasks / Subtasks

- [ ] **Task 1: Initialize Next.js 15 Project** (AC: 1)
  - [ ] Run `npx create-next-app@latest` with options: TypeScript, Tailwind CSS, App Router, src directory NO, use /app directory YES
  - [ ] Verify Next.js version is 15.x in package.json
  - [ ] Verify React version is 19.x in package.json
  - [ ] Configure path aliases in tsconfig.json (@/components, @/lib, @/app)
  - [ ] Test dev server runs: `npm run dev`

- [ ] **Task 2: Configure Tailwind CSS with KB-COM Brand Colors** (AC: 2)
  - [ ] Update tailwind.config.ts with KB-COM color palette from front-end-spec.md
  - [ ] Add primary colors: main (#3a67ff), light (#6b8aff), dark (#2a4fd9)
  - [ ] Add gray scale: 50, 100, 300, 500, 700, 900
  - [ ] Add semantic colors: success, warning, error, info
  - [ ] Add dark mode colors configuration
  - [ ] Configure custom spacing (8px grid system)
  - [ ] Configure custom border radius (16px for cards, 12px for buttons)
  - [ ] Add Inter font family configuration
  - [ ] Test Tailwind compilation works

- [ ] **Task 3: Install and Configure shadcn/ui** (AC: 3)
  - [ ] Run `npx shadcn-ui@latest init` with default options
  - [ ] Install Button component: `npx shadcn-ui@latest add button`
  - [ ] Install Form component: `npx shadcn-ui@latest add form`
  - [ ] Install Card component: `npx shadcn-ui@latest add card`
  - [ ] Verify components are in components/ui/ directory
  - [ ] Verify lib/utils.ts with cn() helper exists
  - [ ] Test render a Button component in app/page.tsx temporarily

- [ ] **Task 4: Configure ESLint with Strict TypeScript Rules** (AC: 4)
  - [ ] Extend Next.js default ESLint config
  - [ ] Add TypeScript strict rules (@typescript-eslint/recommended-type-checked)
  - [ ] Add unused import detection rules
  - [ ] Configure .eslintignore for node_modules, .next, build artifacts
  - [ ] Add npm script: `"lint": "next lint"`
  - [ ] Run lint and fix any initial issues

- [ ] **Task 5: Configure Prettier for Code Formatting** (AC: 5)
  - [ ] Install prettier and eslint-config-prettier
  - [ ] Create .prettierrc with settings: semi: true, singleQuote: true, tabWidth: 2, trailingComma: 'es5'
  - [ ] Create .prettierignore for node_modules, .next, build artifacts
  - [ ] Add npm script: `"format": "prettier --write ."`
  - [ ] Add npm script: `"format:check": "prettier --check ."`
  - [ ] Run format on entire codebase

- [ ] **Task 6: Set Up Husky Pre-commit Hooks** (AC: 6)
  - [ ] Install husky: `npm install --save-dev husky`
  - [ ] Initialize husky: `npx husky init`
  - [ ] Create .husky/pre-commit hook with lint-staged
  - [ ] Install lint-staged: `npm install --save-dev lint-staged`
  - [ ] Configure lint-staged in package.json to run ESLint and Prettier on staged files
  - [ ] Add type-check script: `"type-check": "tsc --noEmit"`
  - [ ] Add type-check to pre-commit hook
  - [ ] Test commit triggers hooks correctly

- [ ] **Task 7: Initialize Git Repository** (AC: 7)
  - [ ] Initialize git: `git init` (if not already done by create-next-app)
  - [ ] Create .gitignore with: node_modules, .next, .env.local, .vercel, build artifacts
  - [ ] Stage all files: `git add .`
  - [ ] Create initial commit: `git commit -m "chore: initialize Next.js 15 project with TypeScript and Tailwind"`

- [ ] **Task 8: Create Comprehensive README.md** (AC: 8)
  - [ ] Add project title: "KB-COM Website - Next.js 15"
  - [ ] Add project description and goals
  - [ ] Add tech stack section listing all technologies
  - [ ] Add prerequisites section (Node.js 20.x, npm 10.x)
  - [ ] Add setup instructions: clone, install, copy .env.example, run dev
  - [ ] Add npm scripts documentation (dev, build, start, lint, format, type-check)
  - [ ] Add project structure overview
  - [ ] Add contributing guidelines (if applicable)
  - [ ] Add license information

- [ ] **Task 9: Create Environment Variables Template** (AC: 9)
  - [ ] Create .env.example file
  - [ ] Add Next.js variables: NEXT_PUBLIC_SITE_URL
  - [ ] Add Sanity placeholder: NEXT_PUBLIC_SANITY_PROJECT_ID, NEXT_PUBLIC_SANITY_DATASET, SANITY_API_TOKEN
  - [ ] Add Supabase placeholder: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
  - [ ] Add Analytics placeholder: NEXT_PUBLIC_GA_MEASUREMENT_ID
  - [ ] Add comments explaining each variable
  - [ ] Document required vs optional variables

- [ ] **Task 10: Configure VSCode Workspace Settings** (AC: 10)
  - [ ] Create .vscode/extensions.json with recommended extensions:
    - ESLint (dbaeumer.vscode-eslint)
    - Prettier (esbenp.prettier-vscode)
    - Tailwind CSS IntelliSense (bradlc.vscode-tailwindcss)
    - TypeScript Vue Plugin (Vue.volar)
  - [ ] Create .vscode/settings.json with:
    - Format on save: true
    - Default formatter: Prettier
    - ESLint auto-fix on save: true
    - TypeScript preferences
    - Tailwind CSS class sorting

- [ ] **Task 11: Unit Tests for Setup Validation** (AC: All)
  - [ ] Create tests/setup.test.ts
  - [ ] Test that Tailwind config loads KB-COM colors correctly
  - [ ] Test that path aliases resolve correctly
  - [ ] Test that cn() utility function works

---

## Dev Notes

### Previous Story Insights

This is the first story in the project. No previous story context exists.

---

### Tech Stack & Versions

[Source: architecture.md#Tech Stack]

**Required Technology Versions:**

- **Next.js:** 15.x (App Router) - Use exact version pinning (e.g., "15.0.3")
- **React:** 19.x - Use exact version pinning, aligned with Next.js requirements
- **TypeScript:** 5.x - Use exact version pinning for consistency
- **Tailwind CSS:** 4.x - Use caret (^4.0.0) for minor updates
- **Node.js:** 20.x (runtime)
- **npm:** 10.x (package manager)

**Component Library:**

- **shadcn/ui + Radix UI:** Latest versions
  - Headless UI patterns with full control
  - Built-in WCAG AA accessibility
  - Copy-paste components (no npm bloat)

**Code Quality Tools:**

- **ESLint:** Latest with TypeScript strict rules
- **Prettier:** Latest for code formatting
- **Husky:** Latest for pre-commit hooks
- **lint-staged:** Latest for running linters on staged files

**Version Locking Strategy:**

- Lock Next.js, React, and TypeScript to exact versions to avoid breaking changes
- Use caret (^) for all other dependencies to allow automatic minor/patch updates
- Configure Dependabot for security patches

---

### Project Structure

[Source: architecture.md#Repository Structure]

**Directory Structure to Create:**

```
kb-com-v3/
├── app/                            # Next.js App Router
│   ├── layout.tsx                  # Root layout (create in future story)
│   ├── page.tsx                    # Homepage (create in future story)
│   └── globals.css                 # Global styles with Tailwind directives
├── components/
│   ├── ui/                         # shadcn/ui components (Button, Form, Card)
│   └── (sections, layout, forms to be added in future stories)
├── lib/
│   └── utils/
│       └── cn.ts                   # Tailwind class merger (from shadcn)
├── public/
│   ├── assets/
│   │   └── logo/
│   │       └── kbcom_logo_dark.svg # Logo already exists
│   └── favicon.ico                 # To be added
├── tests/
│   ├── unit/                       # Vitest unit tests
│   ├── integration/                # Integration tests (future)
│   └── e2e/                        # Playwright tests (future)
├── .vscode/
│   ├── extensions.json
│   └── settings.json
├── .github/
│   └── workflows/                  # CI/CD (Story 1.2)
├── .env.example                    # Environment variables template
├── .env.local                      # Local secrets (gitignored)
├── .gitignore
├── .eslintrc.json
├── .prettierrc
├── .prettierignore
├── next.config.ts                  # Next.js configuration
├── tailwind.config.ts              # Tailwind CSS configuration
├── tsconfig.json                   # TypeScript configuration
├── package.json
├── package-lock.json
└── README.md
```

**Path Aliases Configuration (tsconfig.json):**

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      "@/components/*": ["./components/*"],
      "@/lib/*": ["./lib/*"],
      "@/app/*": ["./app/*"]
    }
  }
}
```

---

### Brand Colors Configuration

[Source: docs/front-end-spec.md#Color Palette]

**Tailwind Config (tailwind.config.ts) - KB-COM Brand Colors:**

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: ['class'],
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#3a67ff',
          light: '#6b8aff',
          dark: '#2a4fd9',
          foreground: '#ffffff',
        },
        gray: {
          50: '#FAFAFA',
          100: '#F5F5F5',
          300: '#D1D1D1',
          500: '#888888',
          700: '#4A4A4A',
          900: '#1A1A1A',
        },
        success: '#27AE60',
        warning: '#F39C12',
        error: '#E74C3C',
        info: '#3a67ff',
        // Dark mode specific colors
        dark: {
          base: '#0A0A0A',
          surface: '#1A1A1A',
          elevated: '#2A2A2A',
          text: {
            primary: '#FFFFFF',
            secondary: '#B3B3B3',
            muted: '#808080',
          },
          border: {
            default: '#2A2A2A',
            elevated: '#3A3A3A',
          },
        },
      },
      spacing: {
        // 8px grid system
        xs: '4px',
        sm: '8px',
        md: '16px',
        lg: '24px',
        xl: '32px',
        '2xl': '48px',
        '3xl': '64px',
        '4xl': '96px',
        '5xl': '128px',
      },
      borderRadius: {
        card: '16px',
        button: '12px',
      },
      fontFamily: {
        sans: ['Inter', '-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'Roboto', 'sans-serif'],
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
};
export default config;
```

---

### ESLint Configuration

[Source: architecture.md#Code Quality]

**Required ESLint Rules:**

- Extend Next.js default ESLint config
- Add TypeScript strict rules: `@typescript-eslint/recommended-type-checked`
- Detect unused imports and variables
- Enforce consistent code style

**.eslintrc.json Example:**

```json
{
  "extends": [
    "next/core-web-vitals",
    "plugin:@typescript-eslint/recommended-type-checked",
    "prettier"
  ],
  "parser": "@typescript-eslint/parser",
  "parserOptions": {
    "project": "./tsconfig.json"
  },
  "plugins": ["@typescript-eslint"],
  "rules": {
    "@typescript-eslint/no-unused-vars": "error",
    "@typescript-eslint/no-explicit-any": "warn",
    "@typescript-eslint/strict-boolean-expressions": "off"
  }
}
```

---

### Prettier Configuration

[Source: architecture.md#Code Quality]

**.prettierrc Example:**

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

**Note:** Install `prettier-plugin-tailwindcss` for automatic Tailwind class sorting.

---

### Husky & lint-staged Configuration

[Source: architecture.md#Code Quality]

**Pre-commit Hook (.husky/pre-commit):**

```bash
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npm run lint-staged
npm run type-check
```

**lint-staged Configuration (package.json):**

```json
{
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
```

---

### Environment Variables Template

[Source: architecture.md#Environment Variables]

**Variables to Include in .env.example:**

```bash
# Next.js
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Sanity CMS (Story 1.3+)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=your_api_token

# Supabase Database (Future Stories)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Upstash Redis (Future Stories)
UPSTASH_REDIS_REST_URL=your_redis_url
UPSTASH_REDIS_REST_TOKEN=your_redis_token

# Email Service - Resend (Future Stories)
RESEND_API_KEY=your_resend_api_key

# Analytics (Story 1.7)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry Error Tracking (Future Stories)
SENTRY_DSN=your_sentry_dsn
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Microsoft Clarity (Future Stories)
NEXT_PUBLIC_CLARITY_PROJECT_ID=your_clarity_id
```

---

### README.md Content Structure

[Source: Epic 1 Story 1.1 Requirements]

**Required Sections:**

1. **Project Title & Description:** KB-COM Website - Next.js 15 with SEO Excellence
2. **Tech Stack:** List all technologies from Tech Stack table
3. **Prerequisites:** Node.js 20.x, npm 10.x
4. **Getting Started:**
   - Clone repository
   - Install dependencies: `npm install`
   - Copy environment variables: `cp .env.example .env.local`
   - Run development server: `npm run dev`
   - Open browser: http://localhost:3000
5. **Available Scripts:**
   - `npm run dev` - Start development server
   - `npm run build` - Build for production
   - `npm run start` - Start production server
   - `npm run lint` - Run ESLint
   - `npm run format` - Format code with Prettier
   - `npm run type-check` - Run TypeScript type checking
   - `npm test` - Run unit tests (Story 1.2+)
6. **Project Structure:** Overview of directory structure
7. **Contributing:** Coding standards, commit conventions
8. **License:** MIT or proprietary

---

### Package.json Scripts

[Source: architecture.md#Build Tool, CI/CD Pipeline]

**Required npm Scripts:**

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "tsc --noEmit",
    "prepare": "husky install"
  }
}
```

---

### Testing

#### Testing Framework Setup (for this story)

[Source: architecture.md#Testing Strategy]

**Testing Tools:**

- **Unit Testing:** Vitest + React Testing Library
- **Test Location:** `tests/unit/` directory
- **Configuration:** Create `vitest.config.ts` in project root

**Vitest Configuration (vitest.config.ts):**

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './'),
    },
  },
});
```

**Dependencies to Install:**

- vitest
- @vitejs/plugin-react
- @testing-library/react
- @testing-library/jest-dom
- jsdom

**Test Coverage Target:**

- Setup validation tests only for this story
- Full coverage (70%+) required starting from Story 1.5+

**Tests to Write:**

1. **tests/unit/setup.test.ts:**
   - Test Tailwind config loads KB-COM colors
   - Test path aliases resolve correctly
   - Test cn() utility function works

**Example Test Structure:**

```typescript
import { describe, it, expect } from 'vitest';
import { cn } from '@/lib/utils';

describe('Setup Validation', () => {
  it('should merge Tailwind classes correctly', () => {
    expect(cn('text-primary', 'bg-gray-100')).toBe('text-primary bg-gray-100');
  });
});
```

---

## Change Log

| Date       | Version | Description                            | Author            |
| ---------- | ------- | -------------------------------------- | ----------------- |
| 2026-01-07 | 1.0     | Story created from Epic 1 requirements | BMAD Scrum Master |

---

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)

### Debug Log References

None - Implementation completed without issues after resolving:

- Tailwind CSS 4.x PostCSS plugin migration
- ESLint 9.x flat config format migration

### Completion Notes

**All Acceptance Criteria Met:**

1. ✅ Next.js 15.5.9 (App Router) with TypeScript 5.6.3 installed
2. ✅ Tailwind CSS 4.1.18 configured with KB-COM brand colors (#3a67ff, #6b8aff, #2a4fd9)
3. ✅ shadcn/ui installed with Button, Card, Form, and Label components
4. ✅ ESLint 9.x configured with flat config format (eslint.config.mjs)
5. ✅ Prettier 3.7.4 configured with Tailwind plugin for class sorting
6. ✅ Husky 9.1.7 pre-commit hooks working with lint-staged
7. ✅ Git repository initialized with 2 commits
8. ✅ README.md created with comprehensive documentation
9. ✅ .env.example template created with all future variables
10. ✅ VSCode workspace settings configured

**Additional Achievements:**

- Vitest 4.0.16 unit testing configured and working (3/3 tests passing)
- Production build successful (102 kB First Load JS)
- All code quality tools integrated and working
- PostCSS configured with @tailwindcss/postcss for v4 compatibility
- Dark mode support configured

**Technical Decisions:**

- Used ESLint 9.x flat config instead of deprecated .eslintrc.json
- Installed @tailwindcss/postcss separately (required for Tailwind v4)
- Configured dark mode as 'class' instead of ['class'] for v4 compatibility
- Used Inter font from Google Fonts (matches design specs)

### File List

**Created Files:**

- `package.json` - Updated with all dependencies and scripts
- `tsconfig.json` - TypeScript configuration with path aliases
- `next.config.ts` - Next.js configuration
- `tailwind.config.ts` - Tailwind with KB-COM colors and spacing
- `postcss.config.mjs` - PostCSS with Tailwind v4 plugin
- `eslint.config.mjs` - ESLint 9.x flat config
- `.prettierrc` - Prettier configuration
- `.prettierignore` - Prettier ignore rules
- `.gitignore` - Git ignore rules
- `.husky/pre-commit` - Pre-commit hook script
- `.env.example` - Environment variables template
- `.vscode/extensions.json` - Recommended VSCode extensions
- `.vscode/settings.json` - VSCode workspace settings
- `README.md` - Comprehensive project documentation
- `vitest.config.ts` - Vitest testing configuration
- `app/layout.tsx` - Root layout with Inter font
- `app/page.tsx` - Homepage placeholder
- `app/globals.css` - Global styles with Tailwind directives
- `components.json` - shadcn/ui configuration
- `lib/utils.ts` - Utility functions (cn helper)
- `components/ui/button.tsx` - Button component (shadcn)
- `components/ui/card.tsx` - Card component (shadcn)
- `components/ui/form.tsx` - Form component (shadcn)
- `components/ui/label.tsx` - Label component (shadcn)
- `tests/setup.ts` - Vitest setup file
- `tests/unit/setup.test.ts` - Setup validation tests

**Modified Files:**

- `docs/stories/1.1.project-setup.md` - Story status updated to Done

---

## QA Results

_To be populated by QA agent after story completion_
